{% for file in files %}
    <input type='button' value='{{ file.name }}' onclick="W.open_by_api('window_css_selected', {data:{template: '{{ template }}', index: '{{ file.index }}' }, url:mod_settings.url_api})"><br>
{% endfor %}